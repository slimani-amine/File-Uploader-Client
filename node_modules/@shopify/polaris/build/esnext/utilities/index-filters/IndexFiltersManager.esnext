import React, { useState, useMemo } from 'react';
import { IndexFiltersModeContext } from './context.esnext';
import { IndexFiltersMode } from './types.esnext';

/** @deprecated No longer needed, will be removing in v12 */
function IndexFiltersManager({
  children
}) {
  const [mode, setMode] = useState(IndexFiltersMode.Default);
  const value = useMemo(() => ({
    mode,
    setMode
  }), [mode, setMode]);
  return /*#__PURE__*/React.createElement(IndexFiltersModeContext.Provider, {
    value: value
  }, children);
}

export { IndexFiltersManager };
